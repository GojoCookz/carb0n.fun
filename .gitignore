# Secrets. Never commit these — the Sepolia deployer key has already been leaked once.
.env
.env.*
!.env.example
*.key
*.pem

# Foundry
contracts/out/
contracts/cache/
# `broadcast/**/dry-run/` and the matching cache entries are where `forge script` writes what it
# calls "Sensitive values", which can include the private key used for a run.
contracts/broadcast/
contracts/**/dry-run/

# Node
node_modules/
app/dist/
app/dist-ssr/
*.local
*.log

# Editor / OS
.DS_Store
.vscode/
.idea/
